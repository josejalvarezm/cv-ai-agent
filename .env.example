# Cloudflare API Credentials
# Get these from: https://dash.cloudflare.com

# API Token with Analytics:Read permission
# Create at: Dashboard → My Profile → API Tokens
# Required permissions:
#   - Account:Analytics:Read
#   - Account:AI Gateway:Read
CLOUDFLARE_API_TOKEN=your_cloudflare_api_token_here

# Your Cloudflare Account ID
# Find at: Dashboard → Workers & Pages → Overview → Account ID (right sidebar)
# Or run: npx wrangler whoami
ACCOUNT_ID=your_account_id_here

# AI Gateway Name (optional, default: cv-assistant-gateway)
# This should match the gateway name you create in Cloudflare Dashboard
GATEWAY_NAME=cv-assistant-gateway
